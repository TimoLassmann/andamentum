# Artefact schemas

Generated by `scripts/schemas.py` — the SAME definition that
`validate_results` enforces. Editing this file by hand will be overwritten;
edit `scripts/schemas.py` instead.

## Common envelope

Every JSON artefact written by `_common.write_json` carries:

| field | type | units | meaning |
|---|---|---|---|
| `schema` | string | - | the artefact's schema id |
| `schema_version` | string | - | the experiment-wide schema generation |
| `written_at` | ISO-8601 UTC | - | when this file was written |
| `provenance_ref` | object | - | {run_id, file, sha256} pointing at results/provenance.json |

## Authoritative timings

In-script `time.monotonic()` deltas are AUTHORITATIVE. Snakemake's `benchmark:` wall-clock is used for cross-checking only, and its memory / IO columns are literally `0` on this macOS host — reporting them as measurements would be fabrication.

## `results/preregistration.json`

- **schema**: `andamentum.experiment.docstore_deferred.preregistration/1`
- **produced by**: `prereg`
- **required keys**: `hypotheses`, `n_hypotheses`

Every hypothesis with its numeric threshold and falsifier, written before any measurement exists. analyze.py may only compare against this file.

| field | type | units | meaning |
|---|---|---|---|
| `hypotheses[].id` | string | - | hypothesis id, e.g. H-c2 |
| `hypotheses[].claim` | string | - | which lettered claim it belongs to |
| `hypotheses[].threshold` | object|null | - | {metric, op, value} |
| `hypotheses[].falsifier` | string|null | - | what would flip it |

## `results/provenance.json`

- **schema**: `andamentum.experiment.provenance/1`
- **produced by**: `provenance`
- **required keys**: `run_id`, `git`, `machine`, `packages`, `ollama`

Run identity: git, versions, model digests, machine, allowlisted env.

| field | type | units | meaning |
|---|---|---|---|
| `run_id` | string | - | '<UTC ISO8601>-<git short sha>' |
| `ollama.pinned_models.<name>.digest` | string | - | THE load-bearing pin: the tag is mutable, the digest is not |
| `git.dirty` | bool | - | whether the working tree had uncommitted changes |
| `environment_allowlisted` | object | - | only DOCUMENT_STORE_DIR / OLLAMA_BASE_URL / TZ / LANG — never the full environ |

## `data/REGISTRY.json`

- **schema**: `andamentum.experiment.docstore_deferred.registry/1`
- **produced by**: `fetch_pdfs`
- **required keys**: `papers`, `registry_version`

Per-paper input provenance. Committed; data/pdfs/ is gitignored.

| field | type | units | meaning |
|---|---|---|---|
| `papers[].arxiv_id` | string | - | VERSIONED id, e.g. 1706.03762v7 |
| `papers[].sha256` | hex string | - | content hash of the downloaded PDF |
| `papers[].bytes` | int | bytes | file size |
| `papers[].final_url` | string | - | URL after redirects |
| `papers[].etag` | string|null | - | upstream ETag, when offered |
| `papers[].download_seconds` | float | s | wall-clock of the fetch |

## `results/data_integrity.json`

- **schema**: `andamentum.experiment.docstore_deferred.data_integrity/1`
- **produced by**: `verify_registry`
- **required keys**: `verdict`, `checks`, `n_failed`

Re-hash of every registered PDF. Exits 1 on drift.

| field | type | units | meaning |
|---|---|---|---|
| `checks[].ok` | bool | - | sha256 and size both match the registry |

## `results/conversion_baseline.json`

- **schema**: `andamentum.experiment.docstore_deferred.conversion_baseline/1`
- **produced by**: `convert_reference`
- **required keys**: `conversions`, `docling_version`

Standalone harvest.extract per PDF: the markdown corpus, and the per-PDF conversion cost that prices the checkpoint.

| field | type | units | meaning |
|---|---|---|---|
| `conversions[].conversion_seconds` | float | s | harvest.extract wall-clock |
| `conversions[].markdown_chars` | int | characters | extracted length |
| `conversions[].markdown_sha256` | hex string | - | valid WITHIN a docling version only — never compared across runs |

## `results/gate_llm.json`

- **schema**: `andamentum.experiment.docstore_deferred.gate_llm/1`
- **produced by**: `gate_llm`
- **required keys**: `verdict`, `topics`, `topics_count`, `extract_chunk_metadata_seconds`

H-G. The blocking precondition that the LLM really enriches.

| field | type | units | meaning |
|---|---|---|---|
| `topics_count` | int | count | non-empty is the pass condition |
| `extract_chunk_metadata_seconds` | float | s | a sub-second value is the signature of the swallowed-provider-error path |
| `resolved_ollama_base_url` | string | - | what the process actually saw |

## `results/claim_a_defer.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_a/1`
- **produced by**: `claim_a_defer`
- **required keys**: `verdict`, `defer_records`, `defer_ollama_requests`, `defer_now_ratio`

H-a1..H-a5: defer is LLM-free, fast, findable — but not for sources.

| field | type | units | meaning |
|---|---|---|---|
| `defer_ollama_requests` | int | count | MUST be 0 (H-a1) |
| `defer_median_seconds` | float | s | median per-document defer cost |
| `defer_now_ratio` | float | ratio | t_defer / t_now — the portable claim |
| `source_probe.self_hits` | int | count | MUST be 0 (H-a5) |

## `results/claim_e_pre.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_e_pre/1`
- **produced by**: `claim_e_pre`
- **required keys**: `verdict`, `probes`, `pre_chunk_recall_at_3`

Claim (e) control arm + the auto_repair tripwire.

| field | type | units | meaning |
|---|---|---|---|
| `pre_chunk_recall_at_3` | float | 0-1 | must be 0 — there are no chunks yet |
| `fingerprint_moved` | bool | - | tripwire: search()'s auto_repair must not drain |

## `results/claim_b_drain.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_b/1`
- **produced by**: `claim_b_drain`
- **required keys**: `verdict`, `drains`, `drain3_enriched`, `drain3_ollama_requests`

H-b: resumable, and a no-op drain is genuinely a no-op.

| field | type | units | meaning |
|---|---|---|---|
| `drains[].fingerprint_pre` | hex string | - | logical fingerprint before |
| `drains[].fingerprint_moved` | bool | - | did the meaning change |
| `drains[].reconciliation.n_mismatches` | int | count | H-x, must be 0 |
| `drain3_ollama_requests` | int | count | the no-op drain's request count |

## `results/claim_e_post.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_e_post/1`
- **produced by**: `claim_e_post`
- **required keys**: `verdict`, `probes`, `post_chunk_recall_at_3`, `per_document`

H-e1/H-e2: the drain created semantic reachability.

| field | type | units | meaning |
|---|---|---|---|
| `post_chunk_recall_at_3` | float | 0-1 | chunk-embedding recall@3 after |
| `doc_embedding_skip_rate` | float | 0-1 | MEASURED not asserted — _embed_doc_level swallows oversized input |
| `public_search_smoke` | object | - | RECORDED, never scored (LLM planner) |

## `results/micro_stages.json`

- **schema**: `andamentum.experiment.docstore_deferred.micro_stages/1`
- **produced by**: `micro_stages`
- **required keys**: `verdict`, `timings_seconds`, `sequential_total_seconds`

Sequential attribution of enrichment, and the fan-out ratio.

| field | type | units | meaning |
|---|---|---|---|
| `timings_seconds.<stage>` | float | s | per-component sequential cost |
| `concurrency_speedup` | float|null | ratio | sequential total / in-pipeline seconds; ~1.0 means Semaphore(5) is decoration |
| `per_chunk_seconds` | float[] | s | the unit of run-to-run variance |

## `results/claim_c_kill.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_c_kill/1`
- **produced by**: `claim_c_kill`
- **required keys**: `verdict`, `pre_kill_markdown_sha256`, `sqlite_integrity`, `hc4_window`

The SIGKILL, the gate that opened it, and the post-kill truth.

| field | type | units | meaning |
|---|---|---|---|
| `pre_kill_markdown_sha256` | hex string | - | H-c2's before value |
| `sqlite_integrity.integrity_ok` | bool | - | H-c3 |
| `hc4_window.observed` | bool | - | the two-transaction window, if seen |

## `results/claim_c_resume.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_c_resume/1`
- **produced by**: `claim_c_resume`
- **required keys**: `verdict`, `ledger_entries`, `ledger_duplicates`, `markdown_sha_stable`

H-c1/H-c2/H-c3/H-d4 and the checkpoint's price in seconds.

| field | type | units | meaning |
|---|---|---|---|
| `ledger_entries` | int | count | MUST equal the number of sources |
| `ledger_duplicates` | string[] | - | MUST be empty |
| `markdown_sha_stable` | bool | - | H-c2, within one run only |
| `checkpoint_savings_seconds` | float | s | conversion seconds not repeated, taken from THIS lineage's own converter-ledger entry. INCLUDES one-time Docling initialisation (that conversion was first in its process); the measured warm cost and docling_init_seconds sit in checkpoint_savings_basis so the marginal saving is legible |
| `repeated_enrichment_seconds` | float | s | MEASURED on this lineage: killed_at minus the converter ledger's last ts_end — the enrichment the resume must redo. Previously this field copied micro_stages' sequential total, a different paper in a different database |
| `repeated_enrichment_over_conversion` | float | ratio | H-d4's second threshold: repeated enrichment must cost less than the conversion the checkpoint preserved |
| `reconciliation.n_mismatches` | int | count | H-x for the subprocess resume drain, which previously bypassed it |

## `results/claim_d_pause.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_d/1`
- **produced by**: `claim_d_pause`
- **required keys**: `verdict`, `arms`, `pause_midstage_documents`

Four pause mechanisms, all stopping between documents.

| field | type | units | meaning |
|---|---|---|---|
| `pause_midstage_documents` | object[] | - | MUST be empty (H-d1). Union over EACH ARM'S OWN post-drain snapshot of rows that are 'failed' or carry chunk/chunk-embedding rows while not 'complete'. Computed per arm on purpose: the snapshot after the final unrestricted drain would have repaired exactly this |
| `pause_discarded_seconds` | float | s | MEASURED: wall-clock each library arm spent after its last committed document, summed. Not a constant |
| `pause_discarded_document_fraction` | float | ratio | the worst arm's post-commit seconds over that drain's own longest single-document cost — H-d1's scored secondary threshold |
| `max_seconds.overrun_seconds` | float | s | H-d2 |
| `sigterm_to_exit_seconds` | float | s | ARM 5: measured with an UNTRUNCATED document in flight. THIS is the number that sizes a launchd/cron shutdown grace period |
| `sigterm_to_exit_seconds_truncated` | float | s | ARM 4: the same measurement on a 6000-char truncation (~2 chunks). Reported for contrast ONLY — NOT operational guidance |
| `sigterm_in_flight_document` | object | - | size of the document arm 5 committed while the signal was in flight, which is what makes the latency interpretable |

## `results/claim_f_fail.json`

- **schema**: `andamentum.experiment.docstore_deferred.claim_f/1`
- **produced by**: `claim_f_fail`
- **required keys**: `verdict`, `failure_fidelity`, `retry_returned`, `stage_test`

H-f1/H-f2: failures recorded and diagnosable; retry routes by stage.

| field | type | units | meaning |
|---|---|---|---|
| `failure_fidelity[].ingest_error` | string | - | verbatim error column |
| `failure_fidelity[].exception_type` | string | - | what a UI can match on |
| `stage_test_converter_calls` | int | count | ALL conversions in the stage-test drain, including the still-broken sources that correctly return to pending_source |
| `stage_test_converter_calls_good_doc` | int | count | conversions of the MARKDOWN-BEARING document — MUST be 0 (H-f2) |

## `results/drain_overhead.json`

- **schema**: `andamentum.experiment.docstore_deferred.drain_overhead/1`
- **produced by**: `drain_overhead`
- **required keys**: `verdict`, `median_seconds`, `median_ollama_requests`

The fixed preflight tax a cron-driven drain pays on every wake-up.

| field | type | units | meaning |
|---|---|---|---|
| `median_seconds` | float | s | across three FRESH processes |
| `median_ollama_requests` | float | count | expected 2: 1 embed + 1 chat |

## `results/claims.json`

- **schema**: `andamentum.experiment.docstore_deferred.claims/1`
- **produced by**: `analyze`
- **required keys**: `rows`, `n_pass`, `n_fail`, `n_inconclusive`

One row per hypothesis: threshold, measured value, verdict.

| field | type | units | meaning |
|---|---|---|---|
| `rows[].id` | string | - | hypothesis id |
| `rows[].threshold` | object|null | - | READ FROM preregistration.json |
| `rows[].measured` | any | - | the observed value |
| `rows[].verdict` | string | - | PASS | FAIL | INCONCLUSIVE | OBSERVATION |
| `rows[].falsifier` | string|null | - | what would have flipped it |

## `results/validation.json`

- **schema**: `andamentum.experiment.docstore_deferred.validation/1`
- **produced by**: `validate_results`
- **required keys**: `verdict`, `violations`

Schema + referential-integrity validation of every results file.

| field | type | units | meaning |
|---|---|---|---|
| `violations` | string[] | - | empty means valid; any entry exits 1 |

## `results/events.jsonl`

- **schema**: `andamentum.experiment.docstore_deferred.events/1`
- **produced by**: `merge_events`
- **required keys**: (none)

Every OBSERVED queue transition, concatenated in rule order. TRAP: documents_skipped uses `continue` WITHOUT incrementing `done`, so on_progress never fires for a skipped source and `done` can end below `total`.

| field | type | units | meaning |
|---|---|---|---|
| `ts_utc` | float | s since epoch | wall-clock of the observation |
| `monotonic_s` | float | s | seconds since the rule started |
| `observer` | string | - | on_progress | poll | report |
| `from_status/to_status` | string|null | - | the transition observed |

## `results/timings.csv`

- **schema**: `andamentum.experiment.docstore_deferred.timings/1`
- **produced by**: `analyze`
- **required keys**: (none)

Per-rule cost roll-up.

| field | type | units | meaning |
|---|---|---|---|
| `rule` | string | - | the producing rule |
| `benchmark_wall_seconds` | float | s | process wall time from Snakemake's benchmark:. Populated for EVERY rule and one single quantity, so it is the only column safe to plot on one axis. Its memory/IO columns are literally 0 on this macOS host and are NOT measurements |
| `in_script_component_seconds` | float|empty | s | in-script time.monotonic, populated only where an artefact carries one. NOT commensurable across rules — for micro_stages it is a SUM OF COMPONENTS, elsewhere a single drain — which is why it is not plotted |
| `in_script_component_meaning` | string | - | which of those two the value is, stated per row |
| `ollama_requests` | int | count | from the http ledger |
| `ollama_seconds` | float | s | summed request latency |
| `max_in_flight_global` | int | count | whole-process depth over all endpoints; per-endpoint peaks live in each artefact's http.max_in_flight_by_path |

## `results/per_document.csv`

- **schema**: `andamentum.experiment.docstore_deferred.per_document/1`
- **produced by**: `analyze`
- **required keys**: (none)

One row per document in the main lineage after the drain.

| field | type | units | meaning |
|---|---|---|---|
| `doc_uuid` | string | - | the document |
| `n_chunks` | int | count | chunks written by enrichment |
| `n_chunk_embeddings` | int | count | must equal n_chunks |
| `has_doc_embedding` | bool | - | measured, not asserted |
| `llm_metadata_populated` | bool | - | persistent trace of the H-G failure |

## `results/MANIFEST.json`

- **schema**: `andamentum.experiment.docstore_deferred.manifest/1`
- **produced by**: `manifest`
- **required keys**: `artefacts`

Every artefact with sha256, bytes, producing rule and schema.

| field | type | units | meaning |
|---|---|---|---|
| `artefacts[].sha256` | hex string | - | content hash |
| `artefacts[].producing_rule` | string | - | which rule wrote it |
| `harness_sha256` | hex string | - | one digest over Snakefile + config.yaml + every scripts/*.py |

## `results/deviations.json`

- **schema**: `andamentum.experiment.docstore_deferred.deviations/1`
- **produced by**: `deviations`
- **required keys**: `deviations`, `n_deviations`

Every change to a hypothesis, metric, threshold or scoring rule made AFTER a measurement was observed. A pre-registration that can be edited after the fact with no amendment trail provides the appearance of the guarantee rather than the guarantee.

| field | type | units | meaning |
|---|---|---|---|
| `deviations[].what` | string | - | the change |
| `deviations[].why` | string | - | what made it necessary |
| `deviations[].affects` | string[] | - | hypothesis ids it could move |
| `deviations[].could_have_moved_the_verdict` | string | - | the honest direction: harder, easier, or neutral |

## `results/cli_smoke.json`

- **schema**: `andamentum.experiment.docstore_deferred.cli_smoke/1`
- **produced by**: `cli_smoke`
- **required keys**: `verdict`, `invocations`, `subcommands_exercised`

Every andamentum-docstore subcommand run through the venv console script: exit code, stdout shape, and `status` output parsed back and compared with the database.

| field | type | units | meaning |
|---|---|---|---|
| `invocations[].argv` | string[] | - | the exact command line |
| `invocations[].returncode` | int | - | process exit code |
| `status_parsed` | object | counts | the `status` output, parsed |
| `status_database_counts` | object | counts | the same counts read straight from sqlite — must agree |

# Glob-keyed artefacts

Written one-per-drain or one-per-rule rather than at a fixed path. Two
structurally different row shapes share `results/ledgers/`, which is why they
are documented as separate globs: `*_convert.jsonl` records converter
invocations, `*_http.jsonl` records HTTP requests.

## `results/ledgers/*_convert.jsonl`

- **schema**: `andamentum.experiment.docstore_deferred.converter_ledger/1`
- **produced by**: `claim_b_drain / claim_c_kill / claim_c_resume / claim_d_pause / claim_f_fail`
- **required keys**: (none)
- **format**: JSONL, one object per line, append-only, fsynced before return

ONE LINE PER convert_fn INVOCATION. The load-bearing instrument for the checkpoint claim: 'conversion is not repeated' reduces to a line count a reader can verify by opening a text file. fsynced before the wrapper returns, which is what makes it valid evidence under a SIGKILL.

| field | type | units | meaning |
|---|---|---|---|
| `ts_start` | float | s since epoch | when the conversion began |
| `ts_end` | float | s since epoch | when it returned or raised |
| `seconds` | float | s | monotonic duration |
| `source` | string | - | the path or URL converted; DUPLICATES FALSIFY H-c1 |
| `pid` | int | - | which process converted it — kill vs resume |
| `chars` | int | characters | length of the markdown produced; 0 on error |
| `sha256` | hex string|null | - | hash of the markdown; H-c2 compares it |
| `error_type` | string|null | - | exception class name, or null on success |
| `error_message` | string|null | - | first 500 chars of the message |

## `results/ledgers/*_http.jsonl`

- **schema**: `andamentum.experiment.docstore_deferred.http_ledger/1`
- **produced by**: `every measurement rule (one ledger per arm)`
- **required keys**: (none)
- **format**: JSONL, one object per httpx request, appended in completion order

Every HTTP request the recording process made, with in-flight depth at start. H-a1 (defer is LLM-free) is the emptiness of one of these files.

| field | type | units | meaning |
|---|---|---|---|
| `ts_start / ts_end` | float | s since epoch | request start / completion |
| `method` | string | - | HTTP method |
| `host` | string | - | netloc; '11434' identifies Ollama |
| `path` | string | - | /v1/chat/completions | /api/embeddings | /api/embed |
| `global_depth_at_start` | int | count | requests open across ALL endpoints when this one started — an upper bound on any single endpoint, never an attribution |
| `in_flight_at_start_path` | int | count | requests open ON THIS PATH when this one started — the per-endpoint concurrency figure |
| `status` | int|null | - | HTTP status, or null if the request raised |
| `error_type` | string|null | - | exception class name on failure |

## `results/snapshots/*.json`

- **schema**: `andamentum.experiment.docstore_deferred.snapshot/1`
- **produced by**: `drain.instrumented_drain (pre and post, one pair per drain)`
- **required keys**: `logical_fingerprint`, `status_counts`, `documents`
- **format**: JSON with the standard envelope

The complete read-only observation of one database at one instant: the logical fingerprint, status counts, and a full row dump. Every headline count in this experiment is re-derivable from these offline.

| field | type | units | meaning |
|---|---|---|---|
| `label` | string | - | which drain and which side (pre / post) |
| `logical_fingerprint` | hex string | - | sha256 over the MEANING of every row (uuid|status|file_hash|markdown_len|n_chunks|has_doc_embedding|n_chunk_embeddings|metadata_keys). The file hash is not used: WAL checkpoints move bytes without changing meaning |
| `status_counts` | object | counts | pending_source / pending_enrich / complete / failed |
| `documents[].ingest_status` | string | - | the row's queue state |
| `documents[].markdown_sha256` | hex string | - | H-c2's byte-identity check |
| `documents[].n_chunks` | int | count | chunk rows written by enrichment |
| `documents[].n_chunk_embeddings` | int | count | must equal n_chunks when complete |
| `documents[].llm_metadata_populated` | bool | - | the H-G failure's persistent trace |
| `file_advisory` | object | - | byte-level facts, ADVISORY — nothing asserts on them |

## `results/events/*.jsonl`

- **schema**: `andamentum.experiment.docstore_deferred.events/1`
- **produced by**: `each claim rule (one file per rule)`
- **required keys**: (none)
- **format**: JSONL, one observed transition per line

The per-rule event log merged by `merge_events` into results/events.jsonl and events.csv. Same field set as that merged file.

| field | type | units | meaning |
|---|---|---|---|
| `ts_utc` | float | s since epoch | wall-clock of the observation |
| `monotonic_s` | float | s | seconds since the rule started |
| `observer` | string | - | on_progress | poll | report |
| `from_status / to_status` | string|null | - | the transition observed |

## `results/events.csv`

- **schema**: `andamentum.experiment.docstore_deferred.events_csv/1`
- **produced by**: `merge_events`
- **required keys**: (none)
- **format**: CSV with a header row

The flat rendering of results/events.jsonl, same fields.

| field | type | units | meaning |
|---|---|---|---|
| `(see results/events.jsonl)` | - | - | identical field set |

## `results/working_tree.patch`

- **schema**: `andamentum.experiment.docstore_deferred.working_tree_patch/1`
- **produced by**: `provenance`
- **required keys**: (none)
- **format**: unified diff (`git diff HEAD`)

WRITTEN ONLY WHEN THE TREE IS DIRTY, and REQUIRED whenever git.dirty is true. A recorded sha with dirty=true and no diff names code that cannot be recovered — the previous edition of this run was measured against an uncommitted one-line fix without which it crashes.

| field | type | units | meaning |
|---|---|---|---|
| `(whole file)` | text | - | hashed into provenance.json as git.working_tree_patch_sha256 |

## `figures/figures_status.json`

- **schema**: `andamentum.experiment.docstore_deferred.figures_status/1`
- **produced by**: `figures`
- **required keys**: `figures`
- **format**: JSON with the standard envelope

Per-figure success/failure. Consulted by build_report, which prints a visible FIGURE COULD NOT BE DRAWN line rather than embedding a placeholder as though it were a result.

| field | type | units | meaning |
|---|---|---|---|
| `figures[].figure` | string | - | the PNG filename |
| `figures[].ok` | bool | - | false means the file is a placeholder |
| `figures[].error` | string|null | - | type and message when not ok |

