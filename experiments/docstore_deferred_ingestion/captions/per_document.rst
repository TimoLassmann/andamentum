**Per-document structure after the drain**: chunk count, chunk-embedding count,
document-embedding presence, metadata keys, final status.

This is the flat, greppable version of the claim (e) structural assertion — the
shape a reader can scan to see that "enriched" means the same thing for every
document, not just in aggregate.
