**The amendment trail** — one row per change made *after* a number had been
seen: what moved, why, which hypothesis ids it could have shifted, and the
honest direction of the shift.

A pre-registration that can be edited after the fact with no trail provides the
*appearance* of the guarantee rather than the guarantee. This file is a
declared artefact, so ``validate_results`` fails the run if it goes missing.
